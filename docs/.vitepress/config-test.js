import { defineConfig } from 'vitepress'

export default defineConfig({
  title: 'V-Tabler',
  description: 'Vue 3 Component Library',
  
  themeConfig: {
    nav: [
      { text: 'Home', link: '/' }
    ]
  }
})
